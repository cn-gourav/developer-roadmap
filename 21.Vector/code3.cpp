#include <iostream>
using namespace std;

int triplet(vector<int> &nums)
{
    vector<int> ans;
    for (int i = 0; i < nums.size(); i++)
    {
        for (int j = 1; nums.size() - 1; i++)
        {
            ans =
        }
    }
}

int main()
{

    vector<int> nums = {-1, 0, 1, 2, -1, -4} triplet() return 0;
}